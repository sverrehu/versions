---
name: Build and test
on:
  pull_request: {}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: '1.27.0'

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
